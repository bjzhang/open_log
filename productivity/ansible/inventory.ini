
# Add hostname we need.
# The user could run in the specfic host in [machines] by the following command:
# <https://ansible-tips-and-tricks.readthedocs.io/en/latest/ansible/commands/>
# ansible-playbook --limit localhost git.yml
[machines]
localhost
dm58

